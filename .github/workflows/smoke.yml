name: Sandbox runner smoke test

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: [self-hosted, vercel-sandbox, Linux, X64]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - run: uname -a
      - run: node --version
