export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", margin: "4rem auto", maxWidth: 720 }}>
      <h1>Vercel Sandbox GitHub Runner</h1>
      <p>
        Configure the GitHub App webhook URL as <code>/api/github/webhook</code>.
      </p>
    </main>
  );
}
